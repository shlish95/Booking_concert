# 콘서트 예약 서비스 테스트 전략

## 1. 문서 목적

이 문서는 테스트 가능한 구조, 테스트 계층 분리, Mock 또는 Stub 활용 방식, 동시성 및 만료 처리 검증 전략을 정리한다.

## 2. 테스트 전략 원칙

- 핵심 비즈니스 로직은 외부 기술과 분리해 단위 테스트 가능하도록 설계한다.
- 테스트는 domain, application, infrastructure 수준으로 나눈다.
- Mock과 Stub은 application 서비스 테스트의 기본 도구로 사용한다.
- 동시성 및 조건부 업데이트는 별도 통합 테스트로 검증한다.

## 3. 계층별 테스트 범위

### 3.1 domain 테스트

대상:

- 상태 전이 규칙
- 만료 판정 규칙
- 결제 가능 여부 판단
- 토큰 사용 가능 여부 판단

특징:

- Spring context 없이 수행한다.
- 빠르고 안정적인 순수 단위 테스트를 지향한다.

예시:

- 만료되지 않은 `HELD` 좌석은 재선점할 수 없다.
- 만료된 임시 예약은 결제할 수 없다.
- `ACTIVE`가 아닌 토큰은 사용할 수 없다.

### 3.2 application 테스트

대상:

- 유스케이스 흐름
- 트랜잭션 안에서 필요한 검증 순서
- 예외 전환
- 이벤트 발행 여부

특징:

- Repository Port, EventPublisher, Clock 등을 Mock 또는 Stub으로 대체한다.
- 외부 기술 없이 유스케이스의 의도와 분기 로직을 검증한다.

예시:

- 좌석 예약 성공 시 예약 생성과 만료 시각 반환
- 좌석 예약 실패 시 `SeatAlreadyHeldException`
- 결제 성공 시 결제 생성, 잔액 차감, 토큰 종료
- 낙관적 락 충돌 시 `OptimisticLockConflictException`

### 3.3 infrastructure 테스트

대상:

- JPA 매핑
- 조건부 업데이트 쿼리
- 스케줄러 동작
- 공통 예외 핸들러

특징:

- `@DataJpaTest`, `@SpringBootTest` 등을 상황에 맞게 사용한다.
- 실제 DB 동작과 쿼리 결과를 확인한다.

예시:

- 좌석 조건부 업데이트가 한 건만 성공하는지
- 낙관적 락 충돌이 실제로 발생하는지
- 만료 스케줄러가 `HELD` 좌석을 `AVAILABLE`로 되돌리는지

## 4. 테스트 가능한 구조 설계

### 4.1 포트 기반 설계

application 서비스는 아래와 같은 포트에 의존하도록 설계한다.

- `QueueTokenReader`
- `QueueTokenWriter`
- `SeatInventoryRepositoryPort`
- `ReservationRepositoryPort`
- `UserBalanceRepositoryPort`
- `PaymentRepositoryPort`
- `BalanceTransactionRepositoryPort`
- `ClockHolder`
- `DomainEventPublisher`

장점:

- 테스트에서 InMemory Stub 또는 Mock 구현으로 쉽게 교체할 수 있다.

### 4.2 Clock 분리

- 현재 시각은 `LocalDateTime.now()`를 직접 호출하지 않는다.
- `ClockHolder` 또는 `TimeProvider` 인터페이스로 추상화한다.

이유:

- 만료 직전, 만료 직후 시나리오를 안정적으로 테스트할 수 있다.

### 4.3 이벤트 발행 분리

- 이벤트 발행은 인터페이스로 추상화한다.
- application 테스트에서는 발행 여부만 검증한다.

## 5. Mock 및 Stub 활용 기준

### 5.1 Mock 권장 대상

- 이벤트 발행기
- 외부 알림 전송기
- 로그성 후처리 컴포넌트

### 5.2 Stub 권장 대상

- 시간 제공자
- 간단한 InMemory Repository
- 테스트 전용 대기열 상태 저장소

### 5.3 Fake 또는 실제 구현 검토 대상

- 좌석 조건부 업데이트
- 낙관적 락 충돌

이유:

- 이 영역은 실제 DB 동작 차이를 많이 타므로 통합 테스트가 더 적합하다.

## 6. 핵심 유스케이스 테스트 목록

### 6.1 대기열

- 토큰 발급 성공
- 콘서트별 대기열이 분리되는지
- `ACTIVE`가 아닌 토큰으로 예약 시 실패하는지

### 6.2 좌석 예약

- 빈 좌석 임시 배정 성공
- 이미 선점된 좌석 예약 실패
- 이미 확정된 좌석 예약 실패
- 만료된 홀드 좌석 재선점 성공
- 한 사용자의 동시 다중 임시 배정 실패

### 6.3 잔액

- 충전 성공
- 잔액 조회 성공
- 결제 시 잔액 부족 실패

### 6.4 결제

- 임시 배정 좌석 결제 성공
- 만료된 예약 결제 실패
- 본인 소유가 아닌 예약 결제 실패
- 이미 결제된 예약 중복 결제 실패

### 6.5 만료 처리

- 스케줄러가 만료된 홀드 좌석을 해제하는지
- 스케줄러가 만료된 예약을 `EXPIRED`로 변경하는지
- 스케줄러가 늦어도 요청 시점 재검증으로 잘못된 결제가 막히는지

## 7. 동시성 테스트 전략

### 7.1 같은 좌석 동시 예약

목표:

- 여러 스레드가 동시에 같은 좌석을 예약 요청할 때 정확히 1건만 성공해야 한다.

검증:

- 성공 건수 1
- 실패 건수 N-1
- 최종 좌석 상태 `HELD`

### 7.2 같은 사용자 다중 결제 시도

목표:

- 같은 사용자가 동시에 여러 결제를 시도할 때 잔액이 음수가 되지 않아야 한다.

검증:

- 낙관적 락 충돌 또는 비즈니스 실패가 발생
- 최종 잔액 정합성 유지

### 7.3 만료 직전 결제

목표:

- 만료 시각 경계에서 잘못된 결제 성공이 발생하지 않아야 한다.

검증:

- 현재 시각 제어
- 만료 이전 성공 또는 이후 실패가 명확히 구분

### 7.4 스케줄러와 요청 충돌

목표:

- 스케줄러가 만료 정리를 수행하는 동안 사용자 요청이 들어와도 최종 상태가 일관돼야 한다.

검증:

- 좌석, 예약, 결제 상태 불일치가 없어야 한다.

## 8. 트랜잭션 실패 및 보상 테스트

검증 대상:

- 결제 생성 직전 실패 시 전체 롤백
- 잔액 차감 후 예외 발생 시 전체 롤백
- 좌석 확정 전 예외 발생 시 예약도 확정되지 않아야 함

비동기 후처리:

- 이벤트 발행 실패는 핵심 트랜잭션과 분리해 재시도 가능한지 검토
- Outbox 패턴 도입 시 별도 통합 테스트 추가 가능

## 9. 예외 처리 테스트 전략

검증 대상:

- 커스텀 예외가 정확한 상황에서 발생하는지
- presentation 계층에서 일관된 에러 코드와 메시지로 변환되는지

예시:

- `QUEUE_TOKEN_NOT_ACTIVE`
- `SEAT_ALREADY_HELD`
- `RESERVATION_EXPIRED`
- `INSUFFICIENT_BALANCE`
- `OPTIMISTIC_LOCK_CONFLICT`

## 10. 로깅 테스트 전략

필수는 아니지만 아래는 검토 가능하다.

- 주요 실패 시 구조화 로그 필드가 누락되지 않는지
- 낙관적 락 충돌 시 경고 로그가 남는지
- 스케줄러 실행 결과 로그가 기록되는지

## 11. 권장 테스트 도구

- JUnit 5
- AssertJ
- Mockito
- Spring Boot Test
- Testcontainers 또는 로컬 DB 기반 통합 테스트

## 12. 우선순위

### 12.1 1순위

- 좌석 중복 선점 방지 테스트
- 결제 트랜잭션 원자성 테스트
- 낙관적 락 충돌 테스트

### 12.2 2순위

- 만료 처리 테스트
- 대기열 토큰 상태 테스트
- 예외 응답 테스트

### 12.3 3순위

- 이벤트 발행 및 후처리 테스트
- 로깅 검증 테스트
