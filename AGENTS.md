# AGENTS.md

## 프로젝트 목적

- 이 프로젝트는 JPA 기반 콘서트 예약 서비스를 학습 목적으로 구현하는 프로젝트다.
- 목표는 기능 구현 자체뿐 아니라, 아키텍처 설계, 동시성 제어, 트랜잭션 처리, 확장 전략을 단계적으로 학습하는 것이다.
- 구현 과정에서 도메인 규칙, 계층 책임, 설계 의도를 코드와 문서에 일관되게 반영한다.

## 아키텍처 원칙

- 본 프로젝트는 도메인 중심의 가벼운 클린 아키텍처 스타일을 따른다.
- 계층은 다음과 같이 구분한다.
- `presentation`: Controller, Request/Response DTO, 공통 예외 응답
- `application`: 유스케이스 조합, 트랜잭션 경계, 흐름 제어
- `domain`: 핵심 도메인 모델, 상태 전이 규칙, 도메인 예외, 정책
- `infrastructure`: JPA Entity, Repository 구현체, Query, Scheduler, 외부 기술 연동
- 의존성은 단방향으로 흐르도록 유지한다.
- `presentation -> application -> domain`
- `infrastructure`는 `application`과 `domain`이 정의한 인터페이스를 구현하는 바깥 계층이다.
- 핵심 비즈니스 로직은 `domain` 또는 `application` 계층에 둔다.
- JPA, HTTP, Redis, Kafka 등의 외부 기술 세부사항이 `domain` 계층으로 침투하지 않도록 한다.

## 도메인 및 설계 원칙

- 좌석 현재 상태의 단일 기준은 `SeatInventory`다.
- `SeatInventory`는 좌석의 현재 상태를 관리한다.
- 예: `AVAILABLE`, `HELD`, `RESERVED`
- 예약 이력과 결제 이력은 별도 도메인에서 관리한다.
- 현재 상태: `SeatInventory`
- 이력과 추적: `Reservation`, `Payment`
- 프로젝트 단순화를 위해 날짜는 회차와 동일한 의미로 본다.
- 한 공연장에서는 하루 하나의 콘서트만 열린다고 가정한다.
- 대기열은 콘서트별 대기열로 관리한다.
- `QueueToken` 상태는 `WAITING`, `ACTIVE`, `USED`, `EXPIRED`를 기본으로 한다.
- 예약과 결제 시에만 유효한 토큰 검증이 필요하다.
- 모든 상태 전이는 코드에서 명시적으로 드러나게 작성한다.

## JPA 및 DB 설계 원칙

- JPA Entity는 `infrastructure` 계층에 둔다.
- 도메인 모델과 JPA Entity는 분리한다.
- JPA Entity 간 연관관계는 최소화한다.
- JPA Entity는 객체 참조 대신 FK ID 필드 중심으로 설계한다.
- 예: `userId`, `concertId`, `scheduleId`, `reservationId`
- DB 테이블 간 외래키 제약은 두지 않는다.
- 참조 무결성 검증 책임은 `application` 서비스와 `domain` 규칙에서 담당한다.
- 무분별한 객체 그래프 탐색보다 명시적인 조회와 조합을 우선한다.

## 동시성 및 트랜잭션 원칙

- 이 프로젝트는 동시성 문제를 단계적으로 학습한다.
- 1. DB 기반 정합성 제어
- 2. Redis 기반 분산 락
- 3. 분산 트랜잭션과 최종 일관성
- 4. Kafka 기반 대용량 비동기 처리
- 초기 구현의 1차 목표는 DB 기반 정합성 제어다.
- 좌석 선점은 조건부 업데이트를 우선 검토한다.
- 잔액 차감은 낙관적 락 `@Version`을 기본안으로 사용한다.
- 결제 확정은 단일 트랜잭션 안에서 상태를 재검증하며 처리한다.
- `한 사용자는 동시에 하나의 임시 배정만 가능` 규칙은 단순 조회 규칙이 아니라 동시성 제약으로 다룬다.
- 필요 시 사용자 기준 락, 보조 테이블, 확장 전략을 검토할 수 있다.
- 만료 처리는 스케줄러와 요청 시점 재검증을 병행한다.
- 스케줄러는 정리 용도다.
- 실제 정합성 판정은 트랜잭션 내 재검증으로 처리한다.

## 대기열 정책 원칙

- 대기열은 콘서트별로 관리한다.
- 각 콘서트는 동시에 작업 가능한 `ACTIVE` 토큰 최대 수를 가진다.
- `ACTIVE` 슬롯이 비면, 대기 순서가 가장 앞선 `WAITING` 토큰을 승격한다.
- `WAITING -> ACTIVE` 승격은 조회 시점 또는 보정 로직, 스케줄러에서 수행할 수 있다.
- 대기열 승격 정책은 문서와 코드에서 일관되게 유지한다.

## 테스트 원칙

- 모든 핵심 기능은 테스트 가능한 구조로 작성한다.
- 단위 테스트는 대상 객체가 필요한 의존성만 가지도록 작성한다.
- 단위 테스트에서는 `Mock`, `Stub`, `Fake`를 적절히 활용한다.
- 도메인 규칙은 도메인 단위 테스트로 우선 검증한다.
- 유스케이스 흐름은 application 서비스 테스트로 검증한다.
- 영속성, 락, 트랜잭션은 통합 테스트에서 검증한다.
- 동시성 이슈는 멀티스레드 기반 테스트 또는 통합 테스트로 재현한다.
- 버그 수정 시 재현 가능한 테스트를 먼저 고려한다.

## 예외 및 로깅 원칙

- 의미 있는 커스텀 예외를 정의한다.
- 예외는 도메인과 애플리케이션 맥락에 맞게 분리한다.
- 예외 응답 형식은 일관되게 유지한다.
- 예약, 결제, 토큰, 만료 처리의 핵심 흐름에는 구조화된 로그 포인트를 둔다.
- 로그에는 상태 추적에 필요한 최소한의 식별 정보만 포함한다.
- 민감정보는 로그에 남기지 않는다.

## 작업 방식 원칙

- 큰 범위를 한 번에 수정하지 않는다.
- 현재 단계의 목표를 벗어나는 기능 추가나 대규모 리팩터링은 하지 않는다.
- 먼저 문서와 현재 구조를 확인한 뒤 구현한다.
- 변경 후에는 다음 내용을 요약한다.
- 변경 이유
- 변경 파일
- 영향 범위
- 확인 방법
- 구현 전에 관련 문서를 먼저 읽는다.
- `README.md`
- `docs/architecture.md`
- `docs/requirements.md`
- `docs/domain-design.md`
- `docs/concurrency.md`
- `docs/transaction-sequence.md`
- `docs/testing-strategy.md`
- `docs/roadmap.md`

## 금지 사항

- 요청 범위를 넘는 리팩터링 금지
- 설명 없는 계층 침범 금지
- 도메인 계층에 JPA, HTTP, Redis 세부기술 직접 도입 금지
- 불필요한 라이브러리 추가 금지
- 민감정보 하드코딩 금지
- 문서와 어긋나는 구조 변경 금지

## 현재 작업 문맥

- 현재 프로젝트는 단계별 학습 로드맵을 기반으로 진행한다.
- 로드맵은 `docs/roadmap.md`를 따른다.
- 각 단계는 별도 브랜치로 관리할 수 있다.
- 각 단계 종료 시 배운 점, 한계, 다음 단계 개선 포인트를 기록한다.
